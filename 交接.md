# 交接：現在在哪、下一步做什麼

2026-08-13 凌晨。設計在 `~/mori-universe/yaze-journal/projects/invisible-watermark-lab/設計.md`。

## 能跑的東西

```bash
cd ~/invisible-watermark-lab
node test/pure.test.mjs     # 11/11,約 1 秒
node test/calibrate.mjs 60  # 量門檻,約 1 分鐘
node test/e2e.mjs           # 真瀏覽器,約 3 分鐘,需要 google-chrome
```

頁面直接開 `index.html` 就能玩（純靜態、無相依、圖片不離開瀏覽器）。流程是
上傳金鑰圖 → 看四張中間產物（原圖／灰階 N×N／指紋／平滑場）→ 上傳作品 → 蓋章
→ 滑桿並排比對 ＋ 差異放大 20 倍 → 驗證 → **拿別人的金鑰偽造**（負控制做成按鈕）
→ 存活測試 13 項。

**還沒推上 GitHub。** repo 是公開的、README 寫了 Pages 網址，但要不要開、什麼時候開是你的決定，我沒有替你按。

## 實作過程推翻的四件事（設計文件要跟著改）

**一、門檻 7 不能沿用。** 那是 8×8 節點、固定 ±1 圖樣時代量的。改成 logo 當金鑰後候選數變 65536，而且兩個隨機零均值場有殘餘相關。實測（各 60 組不相干金鑰）：

| 節點 | 正向 | 乾淨圖 | 拿別人金鑰驗 | 新門檻 |
|---|---|---|---|---|
| 8×8 | 31.6 | ≤ 1.3 | 中位 12.2 / 最高 18.1 | 28 |
| 16×16 | 31.6 | ≤ 1.5 | 中位 8.0 / 最高 10.2 | 16 |

**假陽性的來源不是乾淨圖**（那個接近 0），是「別人的金鑰撞到你的場」。

**二、8×8 不只是「logo 認不出來」，是明顯比較好偽造。** 設計文件第 3 節把它寫成純粹的「解析度換強健度」，要改。8×8 的假陽性離真陽性太近。

**三、位移搜尋要折疊。** 原本 N²×格數，N=16 時上億次，按一下等十幾秒。改成先把高通值依 `(i%N, j%N)` 折疊成 N×N 累加表，再跟模板做環狀互相關——數值完全等價（回歸測試 z 一位不差），成本與圖片大小脫鉤。整包測試 3.9 秒 → 1.0 秒。

**四、logo 場比 ±1 圖樣弱很多，兩個原因都修了。**
- 偵測端第一步是高通，自然圖片是低頻的，相鄰節點高度相關 → 指紋要先白化
- 峰值正規化沒用滿振幅預算 → 改 RMS 正規化（±1 圖樣正好是 RMS=1 的極端，隱形性有現成證據，所以對齊到 RMS=1 不會比已知安全的基準更明顯）

demo 圖的 z：17.2 → 25.0，JPEG 90 從死變活。

## 沒解決的問題（明天第一件事）

**乾淨的幾何 logo 太弱。** 隔離測試（純 node，排除 canvas）：

| 金鑰 | 原圖 | 裁一半 | 裁到 30% |
|---|---|---|---|
| 隨機圖樣（本來就是白的） | 43.0 | 19.4 ✓ | 11.0 ✗ |
| 頁面的幾何示範 logo | 25.0 | 5.6 ✗ | 6.3 ✗ |

圓形＋方塊＋三角這種乾淨 logo，白化之後只剩邊緣有能量，等效自由度很低。**而人們上傳的正是這種乾淨 logo。**

三個方向，我沒有替你選：

1. **接受並誠實標示**：強度計改成量「等效自由度」而不是 RMS，弱的金鑰直接告訴使用者「這張圖當金鑰會撐不過裁切」，並建議加細節。最誠實，但很多人會卡在第一步。
2. **金鑰圖 → 偽隨機種子**：拿 logo 的雜湊當種子產生 ±1 圖樣。強度立刻回到 43，但「你的 logo 就是指紋」這個教學上最有感的部分會消失——指紋圖看起來會跟 logo 完全無關。
3. **折衷**：logo 決定低頻結構、雜湊種子補高頻，兩者相加。強度回來，指紋圖仍隱約看得出 logo。實作最複雜，要重新校準門檻。

**我的建議是 3**，因為這個 repo 的賣點就是「你的圖變成指紋」的那個瞬間；但 3 要多花一輪校準，而且要重新量整張存活表。

## 其他待辦

- 存活矩陣目前 13 項有 6 項跟預期不符（表在 `test/e2e.mjs` 輸出）。預期值是照設計文件第 6 節寫的，那些是 ±1 圖樣的數字。**等金鑰強度問題定案後要整張重量、重寫預期**，不要現在改預期去迎合結果。
- `calibrate.mjs` 的門檻是在「隨機金鑰」下量的，白化與 RMS 正規化之後還沒重跑，數字要重驗。
- README 的存活表還是空的，等重量之後補。
- promo footer 目前是手寫的三連結，還沒套 `promo-footer` skill 的正式模板（BMC 彈跳對話泡沒做）。
- 沒有 PWA／離線，設計文件也沒要求，先不做。
