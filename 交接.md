# 交接：現在在哪、下一步做什麼

2026-08-13。設計在 `~/mori-universe/yaze-journal/projects/invisible-watermark-lab/設計.md`（8/11 版，**已經有多處被實測推翻，見下**）。

**線上：** https://yazelin.github.io/invisible-watermark-lab/
**repo：** https://github.com/yazelin/invisible-watermark-lab （公開）

```bash
node test/pure.test.mjs     # 16/16,約 1 秒
node test/e2e.mjs           # 真瀏覽器,約 3 分鐘,需要 google-chrome
PAGE=https://yazelin.github.io/invisible-watermark-lab/ node test/e2e.mjs   # 打線上版
node test/calibrate.mjs 60  # 註:這支還是 v1 的,見待辦
```

## v2 定案的設計

**金鑰是一段 secret，不是 logo。** `IWL1-XXXXX-XXXXX-XXXXX-XXXXX`（Crockford base32，100 bit）餵給 xmur3+sfc32 產生 16×16 的 ±1 指紋。

**鑰圖是容器。** secret 在圖裡存三份：PNG iTXt 中繼資料（主要）、QR（截圖也救得回來）、明文（用眼睛抄）。警告烙在像素裡而不是寫在頁面上——說明沒人看，但圖會跟著圖跑。logo 放在鑰圖上當**標籤**，對演算法零貢獻，頁面明講。

**判定是雙門檻**：`z > 6` 且 `PSR > 6.5`。

## 實測推翻設計文件的地方

| 設計文件說 | 實測 |
|---|---|
| 門檻 7 | 那是 8×8、固定 ±1 時代量的。v1 假陽性最高 10.2；v2 改雙門檻 |
| 8×8 只是「logo 認不出來」的取捨 | 8×8 明顯比較好偽造（假陽性中位數 12.2 對 8.0） |
| logo 當金鑰 | 兩個定義上補不掉的問題（見下），已放棄 |
| 位移搜尋 | 天真寫法 N²×格數，N=16 時上億次。折疊成環狀互相關後數值一位不差、快 40 倍 |

**logo 當金鑰為什麼不行：**

- 公開的東西不能當金鑰
- 自然圖片是低頻的，偵測端第一步就高通，能量被殺掉大半。正向 25.0 對 43.7、裁一半 5.6 對 19.8

**雙門檻為什麼必要**（兩種假陽性的破口剛好相反）：

| | z | PSR |
|---|---|---|
| 正向 | 43.7 | 13.7 |
| 裁一半 | 19.8 | 12.5 |
| 乾淨圖 | 1.4 低 | **10.1 高** |
| 別人的金鑰 | **13.2 高** | 4.4 低 |

18/18 真陽性、90/90 真陰性、零誤判。

**還原**原本用「蓋一個相反的場」，只回得去 64.7%（`Math.floor` 與近白減半不可逆）。改成逐像素反解 `b0±3`，現在 100% 逐像素相同。

## 存活矩陣（2026-08-13 重量，`test/survive-matrix.mjs`）

四種代表性圖片各一把新金鑰。**「高細節密集紋理」連原圖都驗不出來（z=6.1）**——那不是攻擊失敗，是 ±2 的訊號從一開始就蓋不過那張圖的雜訊底，所以列進界限、不列進預期。預期取自其餘三張，三張全活才寫 true。

跟上一版（抄自設計文件、在另一張圖上量的）三個差別：

- **裁切 true → false**：裁一半只有大面積淺色那張活，平滑漸層與實拍照片都死
- **JPEG 50 false → true**：三張都活
- **縮到 25% 死 → 活**：那是 bug 不是訊號沒了。尺度搜尋上限抄自 line-chat-maker 的 ×2.6，但還原 25% 需要 ×4。補到 ×6 之後回來（24.9／26.3／38.0）

改完 e2e 跑起來是 **13 項零項不符**。

## 2026-08-13 補的

- **存活矩陣加到 15 項**：補了「裁一半再縮一半」「裁到 30% 再縮到 40%」——截圖再上傳是最真實的濫用路徑，本來只測單一手段。兩項都是 1/4（只有大面積淺色活），跟單純裁切一致：決定命運的是雜訊底，不是被切掉多少。
- **縮圖改成保留相對比例**：原本每張都塞滿固定寬度，等於把「被縮小」「被裁掉」抹掉，所有列看起來一樣大。現在同一個框、按真實比例畫、置中，裁切（部分內容＋小框）跟縮放（完整內容＋小框）一眼分得出來。

## 2026-08-13 第三輪：兩把梯子，量出一個反直覺的結果

存活矩陣擴到 21 項，加了兩把梯子讓人看得出「能切多少會活」：

| | 90% | 75% | 60% | 50% | 30% |
|---|---|---|---|---|---|
| 純裁切 | 活 | 活 | **死** | 死 | 死 |
| 裁完再縮一半 | 活 | 活 | **活** | 死 | — |

**「裁到 60%」單獨會死，但「裁到 60% 再縮一半」反而活。**多做一道破壞卻活下來，因為縮小再放大等於過了一次低通濾波：場紋是 16 像素節點的平滑訊號，重新取樣殺不掉；畫面內容的高頻雜訊卻被平均掉了，訊噪比因此變好。

所以原本寫的「縮放是可還原的、不會丟資訊」只講對一半——它不只不丟，還會幫忙。頁面與 README 的界限那條已照這個結果改寫。

**這一項的 expect 是量完才改的**（我原本照假設填 true，量出來 1/3 就改成 false），不是先寫結果再湊。

## 待辦

（清空了。三件都做完：`calibrate.mjs` 已改成 v2 並成為 `Z_MIN`／`PSR_MIN` 的出處；示範圖雜訊改固定種子，兩次載入的像素雜湊一致；設計文件已照實測改寫，並新增第 11 節「實作之後修正了什麼」。）
